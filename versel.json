{
    "version": 2,
    "builds": [
        {
            "src": "./src/app.js",
            "use": "@versel/node"
        }
    ],
    "routes": [
        { "src": "/(.*)", "dest": "/app.js" }
     ]
}